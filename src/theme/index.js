import { createTheme } from "@mui/material"

export const theme = createTheme({
    palette: {
        primary: {
            light: '#DCDDF5',
            main: '#585886',
            dark: '#3E3E5F'
        }, 
        secondary: {
            main: '#EEEEF3'
        }
    },
});